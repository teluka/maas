diff --git a/preseed_network.py b/preseed_network.py.new
index c91b74b..f096ea3 100644
--- a/preseed_network.py
+++ b/preseed_network.py.new
@@ -255,7 +255,7 @@ class InterfaceConfiguration:
         return {
             route
             for route in self.routes
-            if route.source == source
+            if str(route.source.cidr) == str(source.cidr)
         }
 
     def _generate_addresses(self, version=1):
